; private extention of platformio.ini

[custom]
# set some configuration parameters for the program
# see config.h for documentation of the various parameters
build_flags = 
	'-D OTA_USERNAME=someuser'
	'-D OTA_PASSWORD=example1'
    '-D MQTT_BROKER="broker.hivemq.com"'
    -D MQTT_PORT=1883
    '-D MQTT_TOPIC_STATUS="marstek-modbus-gateway/status"'
    '-D MQTT_TOPIC_COMMAND="marstek-modbus-gateway/command"'
    '-D MQTT_USERNAME="dummy"'
    '-D MQTT_PASSWORD="dummy"'
    ; Uncomment to enable live sequence recording (publishes CSV rows to marstek-modbus-gateway/sequence)
    ; -D MQTT_SEQUENCE_RECORD
